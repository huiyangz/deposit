# BR002 计算账户利率

- 规则类型：计算类

## 规则描述

a.若{账户利率浮动百分点}不等于0，那么返回[执行利率]等于{产品利率}+{账户利率浮动百分点}；b.若{账户利率浮动百分比}不等于0，那么返回[执行利率]等于{产品利率}*(1+{账户利率浮动百分比})；c.若{账户固定利率}不等于0，那么返回[执行利率]等于{账户固定利率}；d.以上条件均不满足时，返回[执行利率]等于{产品利率}；多个条件同时满足时，按a、b、c顺序取先命中的分支计算

## 输入

| 字段名 | 类型 | 标记 | 来源实体 | 说明 |
|--------|------|------|----------|------|
| acctSpreadRate | java.math.BigDecimal | 非必填 | | 账户利率浮动百分点，空值视同0 |
| acctPercentRate | java.math.BigDecimal | 非必填 | | 账户利率浮动百分比，空值视同0 |
| acctFixedRate | java.math.BigDecimal | 非必填 | | 账户固定利率，空值视同0 |
| productRate | java.math.BigDecimal | 必填 | | 产品利率 |

## 输出

| 字段名 | 类型 | 标记 | 来源实体 | 说明 |
|--------|------|------|----------|------|
| realRate | java.math.BigDecimal | 非必填 | | 执行利率，保留小数点后6位，四舍五入 |

## 提示

- 可能相关的属性：账户利率浮动百分点，类型为：java.math.BigDecimal
- 可能相关的属性：账户利率浮动百分比，类型为：java.math.BigDecimal
- 可能相关的属性：账户固定利率，类型为：java.math.BigDecimal
