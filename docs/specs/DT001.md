# DT001 根据核准类型设置账户状态

- 规则类型：决策类

## 规则描述

1.若{账户属性}为“基本户”，则：
a.若{境内境外标志}为“境内”且{企业标志}为“是”，则返回[账户状态]为“新建”
b.若{境内境外标志}为“境内”且{企业标志}为“否”，则返回[账户状态]为“预开户”
c.若{境内境外标志}为“境外”且{企业标志}为“是”，则返回[账户状态]为“预开户”
d.若{境内境外标志}为“境外”且{企业标志}为“否”，则返回[账户状态]为“新建”
2.若{账户属性}为"一般户"，则返回[账户状态]为"新建"
3.若{账户属性}为"专用户"，则：
3.1.若{账户用途}为"预算单位专用存款户"：

a.若{境内境外标志}为"境内"且{企业标志}为"是"，则返回[账户状态]为"无"
b.若{境内境外标志}为"境内"且{企业标志}为"否"，则返回[账户状态]为"预开户"
c.若{境内境外标志}为"境外"且{企业标志}为"是"，则返回[账户状态]为"无"
d.若{境内境外标志}为"境外"且{企业标志}为"否"，则返回[账户状态]为"新建"
3.2.若{账户用途}为"非预算单位专用存款户"或其他取值（含为空），则返回[账户状态]为"新建"

4.若{账户属性}为"临时户"，则：
a.若{境内境外标志}为"境内"且{企业标志}为"是"，则返回[账户状态]为"新建"
b.若{境内境外标志}为"境内"且{企业标志}为"否"，则返回[账户状态]为"预开户"
c.若{境内境外标志}为"境外"且{企业标志}为"是"，则返回[账户状态]为"无"
d.若{境内境外标志}为"境外"且{企业标志}为"否"，则返回[账户状态]为"新建"
5.若{账户属性}为"验资户"，则：
a.若{境内境外标志}为"境内"且{企业标志}为"是"，则返回[账户状态]为"新建"
b.若{境内境外标志}为"境内"且{企业标志}为"否"，则返回[账户状态]为"新建"
c.若{境内境外标志}为"境外"且{企业标志}为"是"，则返回[账户状态]为"无"
d.若{境内境外标志}为"境外"且{企业标志}为"否"，则返回[账户状态]为"新建"
6.否则，返回[账户状态]为“新建”

## 输入

| 字段名 | 类型 | 标记 | 来源实体 | 说明 |
|--------|------|------|----------|------|
| rbBusAcctPurpose | com.dcits.deposit.enums.RbBusAcctPurpose | 非必填 | | 对公存款账户用途 |
| acctNatureNo | com.dcits.deposit.enums.AcctNatureNo | 必填 | | 账户属性 |
| corporationFlag | java.lang.String | 必填 | | 企业标志 |
| inlandOffshore | java.lang.String | 必填 | | 境内境外标志 |

## 输出

| 字段名 | 类型 | 标记 | 来源实体 | 说明 |
|--------|------|------|----------|------|
| acctStatus | com.dcits.deposit.enums.AcctStatus | 非必填 | 对公存款账户主表（RB_BUS_ACCT） | 账户状态，"新建"对应 N，"预开户"对应 I，"无"表示不设置、返回空 |

## 相关枚举

| 枚举 | 别名 |
|------|------|
| 对公存款账户用途 | 对公存款账户用途 |
| 账户属性 | 账户属性 |

## 提示

- 可能相关的属性：企业标志，类型为：java.lang.String
- 可能相关的属性：境内境外标志，类型为：java.lang.String
- {对公存款账户用途,对公存款账户用途}取值来源于代码[对公存款账户用途]，对应枚举类-类名：com.dcits.deposit.enums.RbBusAcctPurpose；"预算单位专用存款户"对应 VALUE_4（预算单位专用，值4），"非预算单位专用存款户"对应 VALUE_3（非预算单位专用，值3）

- {账户属性,账户属性}取值来源于代码[账户属性]，对应枚举类-类名：com.dcits.deposit.enums.AcctNatureNo；"基本户"对应 VALUE_11001（基本存款账户，值11001），"一般户"对应 VALUE_11002（一般存款账户，值11002），"专用户"对应 VALUE_11004（专用存款账户，值11004），"临时户"对应 VALUE_11003（临时存款账户，值11003），"验资户"对应 VALUE_17（验资户，值17）
